function pokaz_chowaj_div_pod_button(el) {
    var element = document.getElementById(el);
    if (element.classList.contains('hidden')) {
        
    }else {
        
    }
    element.classList.toggle('hidden');
}